import { useRef } from "react";
import { Button } from "../Button";

export const MetamaskProvider = () => {
  const onClick = () => {};

  return (
    <Button color="outline" onClick={onClick}>
      Metamask
    </Button>
  );
};
